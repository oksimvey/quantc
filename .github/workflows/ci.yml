name: QuantC CI

on:
  push:
    branches: [main, feature/split-codegen-dynamic-auto]
  pull_request:
    branches: [main]

jobs:
  build-and-smoke-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure with Clang
        run: cmake -S . -B ci-build -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER=clang++

      - name: Build QuantC
        run: cmake --build ci-build --config Release

      - name: Transpile dynamic auto test
        run: ./ci-build/QuantC tests/dynamic/main.qc -o ci-build/generated

      - name: Verify split output
        run: |
          test -f ci-build/generated/main.cpp
          test -f ci-build/generated/Dog.h
          grep -q 'std::variant<std::string, Dog> dog' ci-build/generated/main.cpp
          grep -q '#include "Dog.h"' ci-build/generated/main.cpp

      - name: Compile generated C++ with Clang
        run: clang++ -std=c++23 ci-build/generated/main.cpp -Ici-build/generated -o ci-build/generated/app

      - name: Run generated program
        run: |
          output="$(./ci-build/generated/app)"
          test "$output" = "Woof"
